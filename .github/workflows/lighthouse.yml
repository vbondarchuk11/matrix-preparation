name: Lighthouse

on:
  # Manual trigger is useful for performance review outside the main CI gate.
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - dev
    # Only run audits when app code or build configuration changes.
    paths:
      - "src/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"
      - "vite.config.ts"
      - "tailwind.config.ts"
      - "lighthouserc.json"

permissions:
  contents: read

jobs:
  audit:
    # Advisory-only audit: useful signal, but not required for merge.
    name: Optional Lighthouse audit
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      # Uses the built static output instead of a separate preview deployment.
      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          configPath: ./lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
